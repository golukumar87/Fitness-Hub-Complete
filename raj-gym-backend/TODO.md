- [x] Confirm backend loads MongoDB Atlas URL and server responds on /health
- [ ] Update frontend (RAJ Project/GYM WEBSITE/script.js) to use backend APIs instead of localStorage for:

  - [ ] signupUser/loginUser (JWT + localStorage token)
  - [ ] progress (GET/PUT)
  - [ ] workout log calendar (GET + PUT per dateKey)
  - [ ] trainer bookings (GET + POST + DELETE)
- [ ] Remove/avoid localStorage usage for those features (keep only UI)
- [ ] Test end-to-end:
  - [ ] Signup/Login
  - [ ] Save Progress and refresh
  - [ ] Save workout log and verify calendar
  - [ ] Book trainer and verify list

